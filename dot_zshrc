# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"
export EDITOR="nvim"   # resolved from PATH, wherever nvim lives on this machine

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time Oh My Zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="powerlevel10k/powerlevel10k"

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# a theme from this variable instead of looking in $ZSH/themes/
# If set to an empty array, this variable will have no effect.
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment one of the following lines to change the auto-update behavior
# zstyle ':omz:update' mode disabled  # disable automatic updates
# zstyle ':omz:update' mode auto      # update automatically without asking
# zstyle ':omz:update' mode reminder  # just remind me to update when it's time

# Uncomment the following line to change how often to auto-update (in days).
# zstyle ':omz:update' frequency 13

# Uncomment the following line if pasting URLs and other text is messed up.
# DISABLE_MAGIC_FUNCTIONS="true"

# Uncomment the following line to disable colors in ls.
# DISABLE_LS_COLORS="true"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# You can also set it to another string to have that shown instead of the default red dots.
# e.g. COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
# Caution: this setting can cause issues with multiline prompts in zsh < 5.7.1 (see #5765)
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# You can set one of the optional three formats:
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# or set a custom format using the strftime function format specifications,
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
# ZSH_CUSTOM=/path/to/new-custom-folder

# FZF_BASE is deliberately unset: the omz fzf plugin auto-detects both the
# Debian layout (/usr/share/doc/fzf/examples) and a brew install.

# NOTE: DISABLE_FZF_AUTO_COMPLETION is deliberately NOT set here -- it does
# nothing on fzf >= 0.48. The plugin's fzf_setup_using_fzf() branch wins first
# and runs `eval "$(fzf --zsh)"`, which emits key-bindings AND completion in one
# blob and never consults that flag. Tab is unbound from fzf further down.

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git sudo zsh-autosuggestions fzf chezmoi aws)

source $ZSH/oh-my-zsh.sh

# User configuration

# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='nvim'
# fi

# Compilation flags
# export ARCHFLAGS="-arch $(uname -m)"

# Set personal aliases, overriding those provided by Oh My Zsh libs,
# plugins, and themes. Aliases can be placed here, though Oh My Zsh
# users are encouraged to define aliases within a top-level file in
# the $ZSH_CUSTOM folder, with .zsh extension. Examples:
# - $ZSH_CUSTOM/aliases.zsh
# - $ZSH_CUSTOM/macos.zsh
# For a full list of active aliases, run `alias`.
#
# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

# PATH first, so everything below can find the tools it probes for.
export PATH="$HOME/.local/bin:$PATH"
[[ -d /opt/nvim-linux-x86_64/bin ]] && export PATH="$PATH:/opt/nvim-linux-x86_64/bin"
[[ -d /usr/local/go/bin ]] && export PATH="$PATH:/usr/local/go/bin"
# rustup already prepends this from ~/.cargo/env via ~/.zshenv, but that file is
# not chezmoi-managed, so repeat it here to keep this dotfile self-sufficient.
# Prepended (not appended) per rustup, so a system rustc can be overridden; the
# ":$PATH:" test keeps it idempotent when ~/.cargo/env has already run.
[[ -d $HOME/.cargo/bin && ":$PATH:" != *":$HOME/.cargo/bin:"* ]] && export PATH="$HOME/.cargo/bin:$PATH"

# Guarded: a machine that hasn't been bootstrapped yet still gets a working shell.
[[ -x /home/linuxbrew/.linuxbrew/bin/brew ]] && eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv zsh)"

# `brew shellenv zsh` ends with `export FPATH`, which leaks fpath into every
# child shell; oh-my-zsh then re-appends its ~11 dirs on top of the inherited
# copy, so fpath grows with each nesting level (54 -> 65 -> 76 ...). That bites
# here because we auto-attach tmux below, so real shells are always nested.
# Drop the export attribute; fpath itself is untouched and stays correct.
typeset +x FPATH

alias ls='eza --icons'

# No compinit here: oh-my-zsh.sh already runs it (with -d "$ZSH_COMPDUMP").
# Calling it a second time wrote a rival ~/.zcompdump and roughly doubled startup.

# Give Tab back to zsh. `fzf --zsh` (run by the omz fzf plugin) ends with
# `bindkey '^I' fzf-completion`, routing every Tab through fzf's widget, which
# only opens the picker on the ** trigger and otherwise delegates to a saved
# fallback -- that hand-off was failing and just ringing the bell. Must come
# after `source $ZSH/oh-my-zsh.sh`, since that is what installs the binding.
# Ctrl-R / Ctrl-T / Alt-C are bound separately and are unaffected.
bindkey '^I' expand-or-complete

# Show system info once per fresh SSH login (not on every nested/tmux shell).
[[ -n "$SSH_CONNECTION" && $SHLVL -eq 1 ]] && command -v macchina >/dev/null && macchina

# mise provides node and the CLI tools listed in ~/.config/mise/config.toml,
# replacing nvm. Guarded so a machine that hasn't been bootstrapped still gets a
# working shell. Shims go on PATH first so child processes that never source this
# file (chezmoi scripts, editors launched from here) still resolve the tools;
# `activate` then prepends the real paths for this shell, which is what gives
# [env] vars and hooks that shims alone cannot.
if (( $+commands[mise] )); then
  eval "$(mise activate zsh --shims)"
  eval "$(mise activate zsh)"
fi

# Land in a persistent tmux session on SSH login, so a dropped mobile connection
# never kills work in progress. -d detaches other clients as we attach, which
# keeps the window at full size for whichever device is actually in use.
# Guarded on $TMUX so tmux's own child shells don't recurse.
if [[ -o interactive && -n "$SSH_CONNECTION" && -z "$TMUX" ]]; then
  tmux attach -d -t main 2>/dev/null || tmux new-session -s main
fi

# Composio CLI
export COMPOSIO_INSTALL_DIR="$HOME/.composio"
[[ -d $COMPOSIO_INSTALL_DIR ]] && export PATH="$COMPOSIO_INSTALL_DIR:$PATH"
